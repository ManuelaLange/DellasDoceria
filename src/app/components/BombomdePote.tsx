interface FlavorPorps {
  name: string;
  price: number;
  description: string;
}

export default function BombomdePote({
  name,
  price,
  description,
}: FlavorPorps) {
  const;
  return (
    <div>
      <h1>Bombom de Pote</h1>
    </div>
  );
}
